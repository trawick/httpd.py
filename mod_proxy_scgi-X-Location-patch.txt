Index: modules/proxy/mod_proxy_scgi.c
===================================================================
--- modules/proxy/mod_proxy_scgi.c	(revision 1672954)
+++ modules/proxy/mod_proxy_scgi.c	(working copy)
@@ -419,6 +419,9 @@
 
     if (conf->internal_redirect && r->status == HTTP_OK) {
         location = apr_table_get(r->headers_out, "Location");
+        if (!location) {
+            location = apr_table_get(r->err_headers_out, "X-Location");
+        }
         if (location && *location == '/') {
             scgi_request_config *req_conf = apr_palloc(r->pool,
                                                        sizeof(*req_conf));
